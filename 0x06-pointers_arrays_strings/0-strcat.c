#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * main-Entry point
 *
 * Descriptioin: To print two diff strings together
 *
 * Return: 0
 */
int main(void)
{

	char *contanate = _strcat(char *dest, char *src);

	reteurn(0);
}
